const analyzesPrompt = `Analyze the following message and determine the user's mood (positive, neutral, or negative) and provide a mood score (-1 for negative, 0 for neutral, 1 for positive). Respond in JSON format with 'mood' and 'score' fields: `;

module.exports = analyzesPrompt;
